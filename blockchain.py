def Main(method, user_pk, business_pk, charity_pk, amount, txid):
    if method == 1:
        # Make structure containing info
    if method == 2:
        # Make method to update structure based on business Neo adding
    if method == 3:
        # Charity gets Neo from completed contracts
